// Comment - ignored by JS engine, purley for documenting code
// This is my first JavaScript code! 
console.log('Hello World');

let myName = "Ashika"; //this variable can be reassigned
console.log(myName);

const interestRate = 0.3; //cannot make changes to constant variable
console.log(interestRate);

//Primitive Types (Value types)

let str = "Ashika"; // String literal
let age = 30; // Number literal
let isApproved = ture; // Boolean literal
let selectedColor = null; // to explicitly clear the value of a variable

//Reference Types

